# OCI Registry Configuration
# Copy this file to .env and customize for your environment
# Then run: source .env && make deploy-all

# OCI Region (e.g., iad, phx, fra)
REGION=iad

# OCI Tenancy Namespace
TENANCY_NAMESPACE=idrdeojypihh

# Repository name/path (without registry URL)
# Example: pif/staging/api
REPO_NAME=pif/staging/api

# Application version (defaults to 'latest')
VERSION=latest

# Helm namespace
HELM_NAMESPACE=moviequiteapi
